apiVersion: v1
kind: Service
metadata:
  name: tique-taque-sync
  namespace: tique-taque-sync
  labels:
    app.kubernetes.io/name: tique-taque-sync
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 8000
      targetPort: http
      protocol: TCP
  selector:
    app.kubernetes.io/name: tique-taque-sync
