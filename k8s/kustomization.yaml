apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: tique-taque-sync

resources:
  - 01-namespace.yaml
  - 02-configmap.yaml
  # Copie 03-secret.example.yaml para 03-secret.yaml e preencha suas credenciais
  # antes do primeiro `kubectl apply -k k8s/` (o arquivo real não é versionado).
  - 03-secret.yaml
  - 04-deployment.yaml
  - 05-service.yaml
  - 06-ingress.yaml
